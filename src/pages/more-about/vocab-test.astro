---
import { getCollection } from 'astro:content';

const vocabularyEntries = await getCollection('vocabulary');
---

<h1>Vocabulary Entries</h1>

<ul>
  {vocabularyEntries.map(entry => (
    <li>
      <strong>{entry.data.title || entry.slug}</strong> — {entry.id}
    </li>
  ))}
</ul>
