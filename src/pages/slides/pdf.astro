---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';

// You can customize the title
const title = "Presentation PDF Export";

// Define frontmatter data for the PDF export page
const frontmatter = {
  title: title,
  description: "Instructions for exporting presentations as PDF files",
  lede: "Instructions for exporting presentations as PDF files"
};
---

<Layout frontmatter={frontmatter}>
  <div class="pdf-export-container">
    <h1>PDF Export Instructions</h1>
    
    <div class="instructions">
      <p>To export your presentation as a PDF:</p>
      
      <ol>
        <li>Click on the link below to open your presentation in PDF mode</li>
        <li>When the presentation loads, press <kbd>Ctrl+P</kbd> (Windows) or <kbd>Cmd+P</kbd> (Mac) to open the print dialog</li>
        <li>Change the destination to "Save as PDF"</li>
        <li>Set the following options:
          <ul>
            <li>Layout: <strong>Landscape</strong></li>
            <li>Margins: <strong>None</strong></li>
            <li>Background graphics: <strong>Enabled</strong></li>
          </ul>
        </li>
        <li>Click "Save" to generate your PDF</li>
      </ol>
      
      <div class="export-links">
        <a href="/slides/?print-pdf" target="_blank" class="btn-primary">Open Main Presentation in PDF Mode</a>
      </div>
      
      <div class="troubleshooting">
        <h2>Troubleshooting</h2>
        <p>If you encounter issues with PDF export:</p>
        <ul>
          <li>Make sure you're using Chrome or Chromium-based browser</li>
          <li>Ensure all presentation content has fully loaded before printing</li>
          <li>Try disabling any browser extensions that might interfere with printing</li>
        </ul>
      </div>
    </div>
  </div>
</Layout>

<style>
  .pdf-export-container {
    max-width: 800px;
    margin: 2rem auto;
    padding: 2rem;
    background-color: var(--clr-primary-bg);
    border-radius: 8px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  }
  
  h1 {
    color: var(--clr-heading);
    margin-bottom: 1.5rem;
  }
  
  .instructions {
    font-size: 1.1rem;
    line-height: 1.6;
  }
  
  ol, ul {
    margin-left: 1.5rem;
    margin-bottom: 1.5rem;
    color: var(--clr-body);
  }
  
  li {
    margin-bottom: 0.5rem;
  }
  
  kbd {
    background-color: #f7f7f7;
    border: 1px solid #ccc;
    border-radius: 3px;
    box-shadow: 0 1px 0 rgba(0,0,0,0.2);
    color: #333;
    display: inline-block;
    font-size: 0.9em;
    font-family: monospace;
    line-height: 1;
    padding: 0.2em 0.4em;
    white-space: nowrap;
  }
  
  .export-links {
    margin: 2rem 0;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  
  .btn-primary {
    display: inline-block;
    background-color: var(--clr-primary);
    color: white;
    padding: 0.75rem 1.5rem;
    border-radius: 4px;
    text-decoration: none;
    font-weight: 600;
    transition: background-color 0.2s ease;
    text-align: center;
  }
  
  .btn-primary:hover {
    background-color: var(--clr-primary-dark, #0056b3);
  }
  
  .troubleshooting {
    margin-top: 2rem;
    padding-top: 1rem;
    border-top: 1px solid #eee;
  }
  
  h2 {
    font-size: 1.5rem;
    margin-bottom: 1rem;
    color: var(--clr-primary);
  }
</style>
